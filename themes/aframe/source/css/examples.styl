@import "_common"

// For Home / Examples pages

body
    color #fff

a
    border-bottom 1px solid rgba(255,255,255,0.15)
    color rgba(255,255,255,0.5)
    text-decoration none
    &:hover
        border-bottom-color rgba(255,255,255,0.3)
        color rgba(255,255,255,0.75)

ul
    list-style-type none

// Extends `_common` styles.
.logo__wordmark
    background url(../images/aframe-name-white.svg) no-repeat

.sidebar
    background $pink
    -webkit-overflow-scrolling touch
    -ms-overflow-style none
    width 260px
    &::-webkit-scrollbar
        width: 0 !important
    .page-title
        font-size 15px
        color rgba(0,0,0,.6)
        font-weight 100
        font-family $mono-font
        letter-spacing 0.02em

.example__iframe
    height 100vh
    width 100%
    background-color #ECECEC

.examples-list
    border-top 1px dashed rgba(255,255,255,1)
    padding-top 20px

.main
    background $pink
    display flex
    height 100%

.sidebar, .content
    position relative

.sidebar
    max-width 400px
    overflow-x hidden

// TODO: Figure out how to hide this scrollbar later. I've done it before.
.sidebar__wrap
    height 100vh
    position relative
    overflow-y auto

.content
    flex 1
    overflow-y auto
    display flex
    flex-direction column

// @media screen and (max-width: 800px)
//     .main
//         flex-direction column

.example__viewsource
    background rgba(0,0,0,0.35)
    color #fff
    display block
    font 12px/1.5 $mono-font
    padding 5px 10px
    position absolute
    text-transform uppercase
    top 20px
    right 20px
    z-index 2
    &:active,
    &:hover
        background-color #ef2d5e

.page__title
    font 600 36px/1.3 $body-font

.docs-section-title
    // padding-left 15px
    position relative
    &:before
        background url(../images/caret.svg) -3px 0 no-repeat
        background-size contain
        // content ""
        position absolute
        top 1px
        height 10px
        left 0
        width 10px

.section-active:before
    transform rotate(90deg)
    top 4px
    left -3px

.nav-link-get-started
    border 2px solid #fff
    color #fff
    display block
    font-size 14px
    font-weight 300
    padding 10px
    text-align center
    transition 0.05es ease-in
    &:hover
        background-color white
        color $pink
        
.subnav-link
    font-size 16px
    font-weight 300
    // border-bottom 1px solid rgba(255,255,255,.1)
    margin-left -3px
    padding 2px 4px 2px 3px
    display block
    transition 0.05s color ease-in
    color #fff
    width 100%
    &:hover
        background-color rgba(0,0,0,0.05)
        // border-bottom 1px solid rgba(255,255,255,.2)
        color #fff
    &:active
        background-color rgba(0,0,0,0.5)
    &.current,
    &.current:hover
        // background-color rgba(255,255,255,0.1)
        font-weight 500;
        color #fff

/*
// Use this version if adding to side bar
.credits
    border-top 1px dashed rgba(255,255,255,0.5)
    background url(../images/mozvr-mozilla.svg) 100% 50% no-repeat
    background-size 100% 100%
    height 60px
    padding-top 20px
    margin 40px 0 20px
*/

// Use this version if adding to right block
.credits
    background url(../images/mozvr-white.svg) 45% 60% no-repeat
    // background-color yellow
    background-size 80% 80%
    position absolute
    bottom 0
    right 0
    height 50px
    width 90px
    margin 0px 20px 20px
    &:hover 
        background-color rgba(0,0,0,0.1)
        

.section-title
    display none

h3 + .examples-subnav
    margin-top 4px

.btn-index
    color rgba(255,255,255,1)

.content--index
    // This extends styles in `_common`.
    flex-direction row
    .example__iframe
        width 65%
        background-color #24CAFF
    .copy__wrap
        width 35%
        min-width 440px
    .slogan
        color #fff
        font-size 2rem
        font-weight 300
        line-height 1.2
        letter-spacing -0.05rem
        // text-transform uppercase
    .intro
        color #fff
        font-size 1.4rem
        font-weight 300
        line-height 1.4

.menu
    margin-bottom 60px

.menu-item
    display inline-block
    a
        font-family $code-font
        text-transform uppercase
        color rgba(0,0,0,0.75)
        margin-right 20px
        border none
    a:hover
        color rgba(0,0,0,1)
        background-color rgba(0,0,0,0.02)
        border-bottom 1px solid rgba(0,0,0,0.5)

.get-started
    background-color white
    border 3px solid white
    color #000
    display inline-block
    font-family $mono-font
    font-size 15px
    font-weight 300
    letter-spacing 0.05rem
    text-align center
    transition 0.05s ease-in
    text-transform uppercase
    padding 8px 30px
    margin-top 2rem
    &:hover
        background-color white
        // border 3px solid #000
        color #000
