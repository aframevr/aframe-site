<% if (projects) { %>
  <h3>Projects</h3>
  <ul>
    <% projects.forEach(function (project) { %>
      <li>
        <%- blog_attribution(project.author) %> built <a href="<%- project.url %>"><%- project.title %></a> &mdash; <%- project.description %>
      </li>
    <% }) %>
  </ul>
<% } %>
