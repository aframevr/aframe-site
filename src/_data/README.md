These files will be loaded into `site.data` for use in templates.

Examples live in `themes/aframe/source/examples.yml`. Don't put them in `src/`
because Hexo has middlewares that will malformat the files.
